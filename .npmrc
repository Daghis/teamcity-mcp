# Supply-chain mitigation: ignore package versions published in the last 7 days.
# Gives the community time to flag compromised/typosquatted releases before we pull them.
# Respected by both pnpm and npm (translates to a `before` registry filter).
min-release-age=7
